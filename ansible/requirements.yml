---
# Install with `ansible-galaxy install -r requirements.yml`
# Update with `ansible-galaxy install -r requirements.yml --force`
collections:
  # provides the `pacman` module, the only non-builtin this playbook uses
  - name: community.general
