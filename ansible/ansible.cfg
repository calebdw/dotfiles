[defaults]
retry_files_enabled = false
inventory = ./inventory/servers.yml
roles_path = ./roles
private_role_vars = true
# Not the mise shim: mise's dir precedes /usr/bin on PATH, and the modules
# want the system python with its distro packages.
interpreter_python = /usr/bin/python3
executable = /bin/bash
